name: Deploy Laravel (Simple & Correct)

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install backend dependencies
        run: |
          cd /var/www/rampo
          composer install --no-dev --optimize-autoloader

      - name: Build frontend
        run: |
          npm install
          npm run build

      - name: Sync project to server
        run: |
          rsync -r \
            --exclude='.git' \
            --exclude='.env' \
            --exclude='storage' \
            --exclude='bootstrap/cache' \
            ./ /var/www/rampo/

      - name: Fix permissions
        run: |
          sudo chown -R www-data:www-data /var/www/rampo/storage /var/www/rampo/bootstrap/cache
          sudo chmod -R 775 /var/www/rampo/storage /var/www/rampo/bootstrap/cache

      - name: Laravel clear & optimize
        run: |
          cd /var/www/rampo
          php artisan optimize:clear